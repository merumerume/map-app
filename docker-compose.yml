services:
  web:
    build: .
    volumes:
      - ./mapproject:/mapproject
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=mapproject.settings
    command: >
      uvicorn mapproject.asgi:application
      --host 0.0.0.0
      --port 8000
      --reload
